# =============================================================================
# 🌟 TÔ DE OLHO - Configurações de Ambiente
# =============================================================================
# Este arquivo contém todas as variáveis de ambiente necessárias para o projeto.
# Copie para .env e ajuste os valores conforme necessário.

# =============================================================================
# 🚀 CONFIGURAÇÕES DO SERVIDOR
# =============================================================================
PORT=8080
GIN_MODE=release
SERVER_READ_TIMEOUT=30s
SERVER_WRITE_TIMEOUT=30s
SERVER_SHUTDOWN_TIMEOUT=10s

# =============================================================================
# 🗄️ CONFIGURAÇÕES DO BANCO DE DADOS (PostgreSQL)
# =============================================================================
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=to_de_olho
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres123
POSTGRES_SSL_MODE=disable

# Pool de conexões
POSTGRES_MAX_CONNS=25
POSTGRES_MIN_CONNS=5
POSTGRES_MAX_CONN_LIFETIME=1h
POSTGRES_MAX_CONN_IDLE_TIME=30m

# =============================================================================
# 🚀 CONFIGURAÇÕES DO CACHE (Redis)
# =============================================================================
REDIS_ADDR=redis:6379
REDIS_PASSWORD=
REDIS_DB=0

# Timeouts
REDIS_READ_TIMEOUT=500ms
REDIS_WRITE_TIMEOUT=500ms
REDIS_DIAL_TIMEOUT=500ms

# =============================================================================
# 🏛️ CONFIGURAÇÕES DA API DA CÂMARA DOS DEPUTADOS
# =============================================================================
CAMARA_API_BASE_URL=https://dadosabertos.camara.leg.br/api/v2
CAMARA_CLIENT_TIMEOUT=30s

# Rate Limiting (respeitar limites da API)
CAMARA_CLIENT_RPS=2
CAMARA_CLIENT_BURST=4

# Retry e Circuit Breaker
CAMARA_CLIENT_MAX_RETRIES=3
CAMARA_CLIENT_RETRY_DELAY=1s

# =============================================================================
# 🎯 CONFIGURAÇÕES DA APLICAÇÃO
# =============================================================================
APP_ENV=development
LOG_LEVEL=info
APP_VERSION=1.0.0

# =============================================================================
# 🌐 CONFIGURAÇÕES DO FRONTEND (Next.js)
# =============================================================================
# IMPORTANTE: NEXT_PUBLIC_* são injetadas no browser, não no servidor!
NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1

# =============================================================================
# 🛠️ CONFIGURAÇÕES DE DESENVOLVIMENTO
# =============================================================================
# Apenas para desenvolvimento local
NODE_ENV=development

# =============================================================================
# 🚀 CONFIGURAÇÕES DE DEPLOY E CI/CD
# =============================================================================
# Para ambientes de produção/staging
# DATABASE_URL=postgresql://user:pass@host:port/dbname?sslmode=require
# REDIS_URL=redis://user:pass@host:port/db
